
.setupRAM
    RST(R1) JMP(.loop);;
    ;; // 1
    ;; // 2
    .loop
        INC(R1) FLG(NE);; // 3
        STR(R1) JMP(.loop); STL(.halt); // 4
        ;; // 5
        SPT(R1);; // 6

.halt
    HLT();;



